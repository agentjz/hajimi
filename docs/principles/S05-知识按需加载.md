# S05 知识按需加载

## 原则

不要一开始把所有知识都塞进 system prompt。

需要时再加载对应 skill。

## 为什么

这样更省上下文，也更接近真正的“按需用知识”。

## 在本项目里

skill 发现和读取在 `src/skills/catalog.ts`，加载工具在 `src/tools/loadSkillTool.ts`。
当 SKILL.md 的 frontmatter 标注 `required: true`，并配置 `trigger`/`triggers` 时，匹配触发词会强制先加载该技能后再继续执行。
