# S06 上下文要能压缩

## 原则

上下文一定会满，所以必须有压缩和续跑机制。

## 为什么

如果没有压缩，长任务跑着跑着就会直接死掉。

## 在本项目里

上下文构建和压缩在 `src/agent/contextBuilder.ts`，主循环续跑在 `src/agent/runTurn.ts`。
压缩采用“三层策略”：总结旧消息、截断旧消息、最小尾部保留，同时把任务状态/待办清单固定在 system prompt 中避免丢失。
